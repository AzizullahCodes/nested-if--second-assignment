// nested if second prcatice assignment

// ============= small-prctice-project-01(even or odd checker)=======================
    //  var enter_number = +prompt('Enter any number');
    // if (enter_number > 0){var check_even = enter_number % 2;
    //      if(check_even === 0){console.log('no is even');}
    //     else{alert('no is odd');}
    // }
    //  else{console.log('Number is negative or zero');
        
    //  }

    // ============= small-prctice-project-02(Grade calculator)=======================
        //     var enter_Marks = +prompt('Enter marks between 0 and 100');
        //     if (enter_Marks >= 0 && enter_Marks <= 100){if(enter_Marks >= 90){alert('Grade A');}
        // else if (enter_Marks >= 80){alert('Grade B');}
        // else if(enter_Marks >= 70){alert('Grade C');}
        // else{alert('Fail');}}

        // ============= small-prctice-project-03(Assignment 3: Login System)=======================

//     var userName = prompt('Enter user name');
//     var enter_Password = prompt('Enter password');
//     var user = 'admin';
//     var pass = '1234';
//     if(enter_Password === pass){if(enter_Password === pass){alert('Login successful');}
//   else if(enter_Password !== pass){alert('Incorrect password');}}
//  else{alert('unknown user');}


// ============= small-prctice-project-04(Assignment 4: Age Group Checker)=======================
        // var age = prompt('Enter your age');
        // if (age >= '0' && age <= 120){if(age < 13){alert('child');}
        // else if (age < 20){alert('Teenager');}
        // else if (age < 60){alert('adult');}
        // else {alert('senior');}}


        // else{alert('Invalid age');}

        // ============= small-prctice-project-05(check vowel or constant without using any fuction or built in method)=======================
    //      var  lr = prompt('Enter a signal alphabet letter like a, b, m, to z etc');
    //      if (lr === 'a' ||lr === 'b' ||lr === 'c' ||lr === 'd' ||lr === 'c' ||lr === 'd' 
    //         ||lr === 'e' ||lr === 'f' ||lr === 'g' ||lr === 'h' ||lr === 'i' ||lr === 'j' 
    //         ||lr === 'k' ||lr === 'l' ||lr === 'm' ||lr === 'n' ||lr === 'o' ||lr === 'p' 
    //         ||lr === 'q' ||lr === 'r' ||lr === 's' ||lr === 't' ||lr === 'u' ||lr === 'v' 
    //         ||lr === 'w' ||lr === 'x' ||lr === 'y' ||lr === 'z' ){
    //          if (lr === 'a' || lr === 'e' || lr === 'i' || lr === 'o' || lr === 'u')
    //             {alert('it is vowel');}
    //          else {alert('it is constant');}
    //  }

    //      else{alert('Invalid entry');}

        // ============= small-prctice-project-06(simple calculator)=======================
//        

    // var number1 = +prompt('Enter a number');
    // var number2 = +prompt('Enter number 2');
    // var operator = prompt('Enter an operator (+,-,*,/)');
    // if (operator === '+' || operator === '-' || operator === '/' || operator === '*'){if (operator === '+'){alert(number1 + number2);}
    // else if (operator === '-'){alert(number1 - number2);}
    // else if (operator === '/'){alert(number1 / number2)}
    // else if (operator === '*'){alert(number1 * number2);}
        
    // }
    // else {alert('Invalid operator');}

     // ============= small-prctice-project-07(Even or Odd and Greater than 100))=======================
            // var enter_number = +prompt('Enter any number');
            // var check = enter_number % 2;
            // if (check === 0){if(check  === 0 && enter_number > 100){alert('Even and greater than 100');}
            // else{alert('Even and less than 100')}}
            // else{alert('No is odd');}

            // ============= small-prctice-project(login system)================================= 
        // var enter_name = prompt('Enter your name');
        // var enter_Password = prompt('Enter your password');
        // var name = 'abcd';
        // var password = '1234';
        // if (enter_name === name){alert('login successful');}
        // else{alert('login fail')}

         // ============= small-prctice-project(access allowed or denied)=================================
    //      var age = +prompt('Enter your age');
    //      if (age >= 18){alert('Access allowed');}
    //      else if (age < 18){alert('Access denied');}
    //   else {alert('Invalid Age Entry')}


      // ============= small-prctice-project(vip access))=================================
    //   var member = ('are you member')
    //   var member = true;
    //   var vip_Pass = false;
    //   if (member){if(vip_Pass){alert('Welcome vip member');}
    // if (member && !vip_Pass){alert('Standard member access');}}
    // else{alert('Guests are not allowed');}
    // var member = prompt('Are you member(yes/not)');
    // var vip_Pass = prompt('have you vip pass(yes/not)');
    // if (member ==='yes' && vip_Pass === 'yes'){alert('welcome vip member');}
    //      else if( member === 'yes' && vip_Pass === 'not'){alert('standard member access');}
    
   
    // else if (member === 'not'){alert('Guests are not allowed');}

// ============= small-prctice-project(grade calculator))=================================
    // var enterPercentage = prompt('Enter percentage(0 to 100)');
    // if(enterPercentage >= 0 && enterPercentage <= 100){if (enterPercentage >= 90){alert('Grade A');}
    // else if (enterPercentage >= 80 && enterPercentage <= 89){alert('Grade B');}
    // else if (enterPercentage >= 70 && enterPercentage <= 89){alert('Grade C');}
    // else if (enterPercentage >= 60 && enterPercentage <= 69){alert('Grade D');}
    // else if (enterPercentage < 60){alert('Grade F');}
    // }

    // else{alert('not valid');}

    //  ============= small-prctice-project(triangle checker)))=================================
//      var angle_1 = +prompt('Enter first triangle');
//      var angle_2 = +prompt('Enter second triangle');
//      var angle_3 = +prompt('Enter third number');
//      var addition = angle_1 + angle_2 + angle_3;
//   if(addition === 180){alert('it is triangle');}

//    else{alert('It is not triangle');}
    // ============= small-prctice-project(grade calculator))=================================
//     📝 Beginner JavaScript Assignment: Operators & Concatenation
// 📌 Part 1: String Concatenation
// Create two variables: firstName and lastName, and assign your name to them.

// Concatenate them into a new variable fullName.

// Print this message using console.log:
// "Hello, my name is <fullName>!"
// var firstName = 'Munir';
// var lastName  = 'khan';
// var fullName = firstName + ' ' + lastName ;
// alert('Hello my name is ' + fullName);

 // ============= small-prctice-project(grade calculator))=================================
//  📌 Part 2: Familiar Operators
// Create two number variables:
// let a = 10;
// let b = 3;
// Print the result of:

// a + b

// a - b

// a * b

// a / b

// a % b
//  ans 

            // let a = 10;
            // let b = 3;
            // var result = a + b;
            // alert(result);
            // var result2 = a - b;
            // alert (result2);

            // var result3 = a * b;
            // alert(result3);

            // var result4 = a / b;
            // alert(result4);

            // var result5 = a % b ;
            // alert(result5);

// ============= small-prctice-project(grade calculator))=================================

// 📌 Bonus Practice (User Input)
// Ask the user for their age using prompt() and show:
// "You are <age> years old!"
// (Use alert() or console.log())

// ans 
// var age = prompt('enter your age');
// alert('you are ' + age + ' years old');

// ============= small-prctice-project(grade calculator))=================================
//  Task 1: Combine First Name, Last Name, and Age
            // var firstName = 'Ajmal';
            // var lastName  = 'Khan';
            // var fullName  =  firstName + ' ' + lastName;
            // var  age     =   28;
            // alert('My full name is ' + fullName + ' and my age is ' + age);

            // ============= small-prctice-project(grade calculator))=================================
            // ➕ Task 2: Calculate Total Cost
            // price = 100, quantity = 5
// totalCost = price * quantity
// Output: "Total cost is: 500"

    // var price = 100;
    // var quantity = 5;
    // var totalPrice = price * quantity;
    // alert('Total cost is: ' + totalPrice);

    // ============= small-prctice-project(grade calculator))=================================
//     🔸 4. Logical NOT (!) Operator
// Task: Use the ! operator to check login status.

// let isLoggedIn = false;

// if (!isLoggedIn) {
//   alert("User is not logged in");
// } else {
//   alert("User is logged in");
// }

// var kid = true;
// if (!kid){alert('you are kid');}
// else{alert('you are not kid');}

// var beautiful = false;
// if (beautiful === true){alert('you are  beautifu');}
// else{alert('not beautiful')}     




